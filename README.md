# homebrew-cflmd

Homebrew tap for `cflmd`.

Install:

```bash
brew tap djnz00/cflmd
brew install cflmd
```

Or install directly from the tap without a separate `brew tap` step:

```bash
brew install djnz00/cflmd/cflmd
```
