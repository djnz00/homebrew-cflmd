# homebrew-djnz00

Homebrew tap for `djnz00` projects.

Currently available formulae:

- `cflmd`

Install:

```bash
brew tap djnz00/djnz00
brew install cflmd
```

Or install directly from the tap without a separate `brew tap` step:

```bash
brew install djnz00/djnz00/cflmd
```
